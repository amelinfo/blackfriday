services:
  postgres:
    image: postgres:16-alpine
    container_name: blackfriday-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: blackfriday
      POSTGRES_USER: blackfriday
      POSTGRES_PASSWORD: blackfriday
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: blackfriday-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  postgres-data:
  redis-data:
